import type { Transaction, InventoryItem, Alert } from './types';

export const mockTransactions: Transaction[] = [];

export const mockInventory: InventoryItem[] = [];

export const mockAlerts: Alert[] = [];
